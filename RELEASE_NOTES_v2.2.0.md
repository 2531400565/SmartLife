# SmartLife v2.2.0 发布说明

> 课表体验升级（Timetable Experience）——周网格总览 + 节次时刻表可配置 + 批量导入增强，让"看课表 / 建课表"一步到位。

## ✨ 新特性

### 1. 课表周网格视图（教务式整周总览）
- 课表页新增第三种视图模式：**列表 / 时间轴 / 周网格** 自由切换
- 周网格为「星期 × 节次段」整周总览，一眼看清一周安排
- 顶部周导航：**上一周 / 下一周 / 回到本周**（1~30 周安全收敛），标题与日期随展示周联动
- 课程按「周次范围 + 单双周」正确落格（如"3-16 周""1-15 单周"只在对应周出现）
- 今日高亮、同格多课自动均分、点格子编辑 / 长按删除，与列表行为一致

### 2. 节次时刻表可配置
- 课表页右上角 ⚙️ 新增「节次时刻表」设置：逐节编辑第 1~10 节起止时间，可一键恢复默认
- 配置存储于 DataStore，不动数据库
- 新增 / 编辑课程对话框出现「第1-2节 08:00-09:35」等快捷填充，一点即填
- 边界说明：节次时刻表只影响**之后**的手录与导入换算，不回写已存课程时间

### 3. CSV 批量导入增强（完整版 10 列）
- 完整版列格式：`课程名,教室,老师,星期,周次,周次范围,课程性质,开始时间,结束时间,考试日期`
  - 星期：`1`（周一）~ `7`（周日），多值用 `|` 分隔（如 `1|3`）
  - 周次：`每周 / 单周 / 双周`（或 EVERY / ODD / EVEN）
  - 周次范围：`1-16`、`3-16`、`11-16`、`1-15`
  - 课程性质：`考试课 / 考查课`（或 EXAM / ASSESSMENT），可留空
  - 时间：`HH:mm`
  - 考试日期：`yyyy-MM-dd`，可留空
- **自动兼容旧版 7 列格式**（按字段数识别），历史导入文件不受影响

## 📄 其它

- README 同步：版本徽章 / 下载表 / Room v4 / 更新日志补 v2.2 条目
- 课程周导航状态扩展：ViewModel 支持按展示周计算周次与日期（周网格跨周浏览正确）

## 📦 下载

- APK：`SmartLife-v2.2.0.apk`（v2 签名，versionCode 5）
