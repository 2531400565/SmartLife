# SmartLife v2.0.0

校园效率工具的一次整体升级：新增**考试中心**、**桌面小组件 2.0**、**数据分析**三大模块，并统一了全 App 的动画节奏。

## 📦 下载

- `SmartLife-v2.0.0.apk`（约 11.5 MB，Android 7.0+，已签名）

安装时如提示「未知来源」，允许即可。首次启动建议在「我的 → 主题」中确认深浅色偏好，并设置「学期开始日期」以启用周次相关功能。

---

## ✨ 新增功能

### 📝 考试中心

独立的考试管理页面，考试信息全部来自课程表中已录入的考试日期，无需重复维护。

- **D-Day 倒计时统计卡**：最近一场未结束考试的剩余天数大数字展示，按紧急程度自动分色（≤7 天红 / 8–30 天橙 / >30 天蓝）
- **顶部统计**：本学期考试总数、已结束数量、最近考试课程名与日期
- **实时搜索**：按课程名即时过滤
- **三档筛选 Tab**：全部 / 未结束 / 已结束（默认进入「未结束」）
- **教室信息**：复用课程表的教室字段，有则显示
- 排序：未结束按剩余天数升序在前，已结束按日期倒序在后

### 🧩 桌面小组件 2.0

原生 `RemoteViews` 实现，不引入 Glance 等额外依赖，深浅色自适应。

| 尺寸 | 内容 | 点击行为 |
| --- | --- | --- |
| 2×2 | 今日待办数 + 下一节课程（时间 / 教室） | 整卡 → 首页 |
| 4×2 | 下一节课程 / 今日待办 / 今日专注时长 / **一键开始专注** | 分区跳转；按钮直达专注页并自动开始（仅空闲状态生效，不会打断进行中的专注） |

刷新机制：系统 30 分钟周期回调 + 应用回到前台主动刷新。

### 📊 数据分析

全部基于已有数据内存计算，**未新增任何数据库字段，Room 结构零改动**。

- **专注趋势**：近 7 天柱状图（今天高亮）+ 近 30 天折线图（Canvas 手绘，含网格线、面积填充、数据点），附总时长 / 日均 / 单日峰值
- **待办效率**：完成率大数字 + 进度条、日均完成数（按最早创建日至今计算）、总待办 / 已完成 / 进行中明细
- **课程分布**：课程总数、当前周次、每周课程数柱状图（单双周课程按周次计入，课程最多的周高亮；仅展示最近 16 周）

### ✨ 动画统一

新增 `ui/theme/Animation.kt` 作为全 App 动画规范的**单一事实来源**：

- 时长三档：`ShortMs=200` / `MediumMs=250` / `LongMs=300`，另有 `FocusRingMs=800`、`CountUpMs=700`
- 统一缓动 `FastOutSlowInEasing`，预置 `enterFadeSlide()` / `exitFadeSlide()`
- 消除全部硬编码时长；页面切换、列表重排、图表生长、数字滚动节奏一致

---

## 🐛 Bug 修复

| 问题 | 影响 | 修复 |
| --- | --- | --- |
| 外部跳转（通知 / 小组件）重复点击同一入口无响应 | 第二次点击后界面毫无反应 | 跳转请求携带单调递增的事件序号，以序号为 `LaunchedEffect` 触发键 |
| 小组件点击「首页」在非首页 Tab 时不跳转 | 误判为「已是起始页」而跳过导航 | 改为对比当前路由并校验路由白名单 |
| 「一键开始专注」二次点击不生效 | 专注结束后再点按钮无反应 | 同样改为序号驱动 |
| 数据分析专注时长按条整除 | 不足 1 分钟的记录被各自截断为 0，多条短会话合计时长凭空丢失 | 先按天累加秒数，最后统一换算为分钟 |
| 学期结束后每周课程数柱状图无限增多 | 柱子挤成一片无法阅读 | 仅展示最近 16 周，标题同步显示实际周次区间 |
| 数据分析页卡片入场时布局跳动 | 延迟期间卡片高度为 0，撑开瞬间下方内容跳动 | 改为始终占位 + 只做透明度 / 位移 |
| `animateItemPlacement` 已弃用 | 编译告警 | 迁移到 `Modifier.animateItem()` |

---

## 🔧 其他改动

- 版本号：`versionCode 2 → 3`，`versionName 1.1 → 2.0.0`
- Release 签名配置：`keystore.properties` + `app/smartlife-release.jks`（均不入库）
- README 全面重写：v2.0.0 功能说明、更新日志、签名构建说明
- 数据库结构未变（Room version 3），旧版本数据可直接升级，无需迁移

---

## ✅ 已知事项

- v2.0.0 新增页面（考试中心 / 数据分析 / 桌面小组件）的截图尚未补齐，README 中已预留位置，待真机实拍后放入 `screenshots/` 目录
- 桌面小组件在部分国产 ROM 上可能受后台限制影响刷新频率，可尝试在系统设置中将应用加入后台保护白名单

**完整更新内容**：`1d76970...v2.0.0`
