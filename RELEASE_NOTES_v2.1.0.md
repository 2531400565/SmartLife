# SmartLife v2.1.0 发布说明

> 课程模型升级（Course Model Upgrade）——让 SmartLife 完整承载学校教务系统课表。

## ✨ 新特性

### 1. 周次范围支持（核心）
- 课程现在可以设置**起始周 / 结束周**（如 1-16 周、3-16 周、11-16 周、1-15 周）
- 课表、首页今日课程、课程提醒、数据分析全部按周次区间过滤，不再把每门课当"全学期课程"显示
- 旧数据自动兼容：迁移后默认按 1-16 周全学期处理，零丢失

### 2. 单双周 × 周次区间叠加
- 单周 / 双周 / 每周 与起止周次可同时生效（如"1-15 周单周"），显示判断严格对齐教务课表

### 3. 课程性质
- 新增课程性质：📘 考试课 / 🟢 考查课 / ⚪ 未知（默认）
- 课表卡片右上角显示课程性质 Badge，使用 Material3 语义色（深浅色模式自动适配）

### 4. 课程编辑页增强
- 新增「周次范围」输入：起始周 / 结束周（1~30，默认 1~16，自动校验 start ≤ end）
- 新增「课程性质」单选

## 🛠 技术变更

- **Room v3 → v4**：`courses` 表新增 `startWeek` / `endWeek` / `courseType` 三列（`MIGRATION_3_4`，SQLite 级迁移，不丢数据）
- `WeekUtils.isActive()` 升级：当前周 ∈ [startWeek, endWeek] 且单双周匹配才显示
- 同步过滤逻辑：课表 / 首页今日课程 / 下一节 / 上课提醒 / 数据分析 / 桌面小组件
- 备份导出/导入兼容新字段，旧备份文件可正常恢复

## 📦 下载

- APK：`SmartLife-v2.1.0.apk`（v2 签名，versionCode 4）
